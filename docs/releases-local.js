// The release being prepared right now, shown on the Releases page before the GitHub release
// exists — it is created when the version tag is pushed. An entry whose tag is already on GitHub
// is ignored, so this never duplicates or overrides a published release.
//
// Generated from RELEASE_NOTES.md by tools/release-feed.py. Edit the notes, not this file.
window.LOCAL_RELEASES = [{
  "tag_name": "v12.1.0-preview.1",
  "name": "AvaTerminal 12.1.0-preview.1",
  "published_at": "2026-08-21T00:00:00Z",
  "html_url": "https://github.com/pavel-zheltiakov/AvaTerminal/releases",
  "body": "## 12.1.0-preview.1 — the first one\n\nThe first public release of AvaTerminal: a terminal emulator control for Avalonia, extracted from a\nterminal workspace application where the parser and the renderer had already been in daily use.\n\n**What works**\n\n- **VT/xterm parsing** — CSI, OSC, DCS and SGR, the alternate screen, scrollback, wide characters\n  and combining marks, reflow on resize. Written here rather than wrapped: there is no libvterm and\n  no xterm.js in the package.\n- **Rendering** — glyph runs through Avalonia's drawing context, cells snapped to whole device\n  pixels, dirty-region repaints, a cursor that blinks at xterm's 530 ms.\n- **Input** — application cursor keys, bracketed paste, mouse modes 1000/1002/1006, and a paste that\n  goes through the platform's own text path so a dead key or an IME composition arrives as the\n  character the user meant.\n- **A pty per program** — `posix_openpt` and `posix_spawn` behind `IPtyHost`, so a program asks the\n  kernel whether it is on a terminal and gets the answer it needs, Ctrl-C is a signal, and resizing\n  raises `SIGWINCH`.\n- **A built-in shell** — `cd`, `pwd`, `exit` and `help`, plus a `PATH` search for everything else.\n  `ls`, `git`, `vim` and `top` all work with no `zsh`, `bash` or `cmd.exe` involved.\n- **A mode with no process at all** — `AutoStart=\"False\"`, then `Feed` bytes in and handle `Input`\n  bytes out. A complete terminal over an SSH channel, a container or a replayed recording.\n\n**What is not here**\n\nPipes, redirection, globbing, variables and job control in the built-in shell — those are a\nlanguage, and a half-implementation that silently ignored `> out.txt` would be worse than an honest\nabsence. Chrome, tabs, splitting and search in the control — those belong to the application.\n\n**Platforms.** macOS and Linux. Windows needs a ConPTY implementation of `IPtyHost`; nothing above\nthat interface changes.\n\n**Preview.** The API may still move before 12.1.0 final. What is most likely to change is the shape\nof `ITerminalView` and the naming around `Start` / `StartBuiltinShell` / `StartSystemShell`."
}];
